# URL-Shortener-Tracker
A mini personal URL manager for the terminal. Shortens any link using TinyURL's free API, saves it locally in a SQLite database with a timestamp and optional note, and displays your full history in a clean Rich table. Everything stays on your machine.
